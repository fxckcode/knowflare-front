import { Chat } from "@/components/chat/chat";

const Page = () => {
  return (
    <main className="max-h-[calc(100dvh-72px)] overflow-hidden">
      <Chat />
    </main>
  );
};

export default Page;